import { FiltraProntasPipe } from './filtra-prontas-pipe';

describe('FiltraProntasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraProntasPipe();
    expect(pipe).toBeTruthy();
  });
});
